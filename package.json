{
  "name": "kkamdol",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "kkamdol archive — public archive of events & media with approval-code writing and anonymous comments",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.4",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.5.4",
    "vite": "^5.4.2"
  }
}
